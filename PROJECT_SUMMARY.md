# 小智 AI 眼镜端项目 - 项目总结（更新版）

## ✅ 项目优化已完成

根据小智官方项目 `https://github.com/78/xiaozhi-esp32` 的实现，我已经将项目配置为**直接连接小智官方服务器**，同时集成了以下优化：

### 🔧 优化内容

#### 1. **OPUS 音频编解码集成** ✅
- 添加 OPUS 编解码库依赖
- 创建 `OpusCodec.kt` 编解码类
- 更新 `AudioService.kt` 集成 OPUS 编码/解码
- 录音时自动编码，播放时自动解码
- 带宽从 256kbps 降低到 20kbps（约 12 倍压缩）

#### 2. **回声消除（AEC）** ✅
- 集成 WebRTC 音频处理库
- 创建 `AudioProcessor.kt` 音频处理器类
- 实时消除麦克风采集到的扬声器回声
- 支持对话模式（CONVERSATIONAL）

#### 3. **噪音抑制（NS）** ✅
- 基于 WebRTC NS 实现
- 支持中等降噪级别
- 有效过滤背景噪音

#### 4. **自动增益控制（AGC）** ✅
- 自动调整麦克风音量
- 目标电平 -3 dBFS
- 支持限幅器防止音量过大

#### 5. **自动对话流程** ✅
- 打开应用自动请求权限
- 权限授予后**自动连接服务器**
- 连接成功后**自动进入监听状态**（无需点击按钮）
- AI 回复完成后**自动进入监听状态**等待用户说话
- 支持**自动重连**（网络断开后 3 秒重试）

#### 6. **静音检测** ✅
- 2 秒静音**自动结束录音**
- 收到音频数据自动重置计时器
- 自动发送停止监听消息

#### 7. **官方服务器配置** ✅
- 默认地址改为 `wss://api.tenclass.net/xiaozhi/v1/`（小智官方服务器）
- 添加标准请求头：`Authorization`、`Protocol-Version`、`Device-Id`、`Client-Id`
- 支持小智 AI 官方服务端协议

### 📁 项目结构

```
xiaozhi-glasses/
├── app/
│   ├── src/main/java/com/rokid/xiaozhi/
│   │   ├── MainActivity.kt              # 主界面（自动开始对话）
│   │   ├── audio/
│   │   │   ├── AudioService.kt          # 音频服务（OPUS + AEC + NS）
│   │   │   ├── OpusCodec.kt             # OPUS 编解码类
│   │   │   └── AudioProcessor.kt        # 音频处理器（AEC + NS + AGC）
│   │   ├── core/
│   │   │   ├── VoiceConversationManager.kt  # 对话管理器（自动流程）
│   │   │   └── ConversationState.kt         # 状态枚举
│   │   └── network/
│   │       └── XiaozhiWebSocketClient.kt    # WebSocket 客户端
│   └── src/main/res/
│       ├── layout/activity_main.xml     # UI 布局
│       └── values/strings.xml           # 字符串资源
├── build.gradle.kts
├── settings.gradle.kts
└── README.md
```

### 🎯 对话流程

```
打开应用 → 请求权限 → 自动连接服务器 → 进入监听状态
    ↓
用户说话 → AEC/NS 处理 → OPUS 编码 → 发送到服务器
    ↓
服务器回复 → OPUS 解码 → 播放 → 设置 AEC 参考
    ↓
AI 回复完成 → 自动进入监听状态 → 等待用户说话
```

### 📋 设备激活流程（首次使用）

**首次连接官方服务器需要完成设备注册：**

```
首次启动应用 → 连接服务器 → 服务器检测未注册设备
    ↓
服务器播放6位数验证码 → 用户在网页输入验证码 → 完成注册
    ↓
退出应用 → 重新启动 → 自动连接并正常使用
```

**详细步骤：**

1. **首次启动应用**
   - 应用自动请求权限并连接服务器
   - 服务器检测到设备未注册，会自动播放 **6位数验证码**（例如：539000）

2. **在网页完成注册**
   - 打开浏览器访问 [https://xiaozhi.me/](https://xiaozhi.me/)
   - 登录或注册账号
   - 进入"设备管理"页面，点击"添加设备"
   - 输入听到的 **6位数验证码**
   - 点击确认完成注册

3. **重新启动应用**
   - 退出眼镜端应用
   - 重新打开应用
   - 应用会自动连接并正常使用

### 🔄 状态机流程

| 状态 | 说明 | 下一个状态 |
|------|------|-----------|
| **IDLE** | 空闲状态 | CONNECTING（自动连接） |
| **CONNECTING** | 连接中 | CONNECTED / ERROR |
| **CONNECTED** | 已连接 | LISTENING（自动监听） |
| **LISTENING** | 正在监听 | THINKING（静音超时） |
| **THINKING** | AI 思考中 | SPEAKING |
| **SPEAKING** | AI 说话中 | CONNECTED（自动监听） |
| **ERROR** | 错误状态 | CONNECTING（自动重连） |

### 📊 技术参数

| 参数 | 值 |
|------|-----|
| 采样率 | 16kHz |
| 位深度 | 16bit |
| 声道 | 单声道 |
| 编码格式 | OPUS |
| 帧时长 | 60ms |
| 比特率 | 20kbps |
| 静音超时 | 2秒 |
| 重连间隔 | 3秒 |
| 协议版本 | v3 |
| 服务器地址 | wss://api.tenclass.net/xiaozhi/v1/ |

### 🔊 音频处理参数

| 功能 | 配置 | 说明 |
|------|------|------|
| AEC 模式 | CONVERSATIONAL | 对话模式，适用于实时语音 |
| NS 级别 | MEDIUM | 中等降噪，平衡音质和降噪效果 |
| AGC 目标电平 | -3 dBFS | 理想的语音信号电平 |
| AGC 压缩增益 | 12 dB | 自动增益控制范围 |
| 限幅器 | 启用 | 防止音量过大导致失真 |

### 🚀 使用说明

1. **首次使用 - 设备激活**
   - 在眼镜上打开应用
   - 应用会自动连接服务器并播放 **6位数验证码**
   - 打开浏览器访问 [https://xiaozhi.me/](https://xiaozhi.me/)
   - 登录账号后进入"设备管理"，添加设备并输入验证码
   - 完成注册后退出应用并重新打开

2. **正常使用**
   - 在眼镜上打开应用
   - 应用会自动请求权限
   - 权限授予后自动连接服务器
   - 状态显示"正在听..."表示可以说话
   - **直接说话即可**（无需点击按钮）
   - 停止说话 2 秒后自动发送
   - AI 回复完成后自动进入监听状态

3. **手动控制**
   - 点击"结束对话"按钮停止对话
   - 点击"开始对话"按钮重新开始

### ⚠️ 注意事项

#### 已解决
- ✅ OPUS 编解码集成
- ✅ 回声消除（AEC）- 基于 WebRTC
- ✅ 噪音抑制（NS）- 基于 WebRTC
- ✅ 自动增益控制（AGC）- 基于 WebRTC
- ✅ 自动对话流程
- ✅ 静音检测
- ✅ 自动重连
- ✅ 连接小智官方服务器 (`wss://api.tenclass.net/xiaozhi/v1/`)

#### 待实现
- ⚠️ 声学回声消除（AEC）可能需要硬件支持

### 📚 参考文档

- **[小智官方网站](https://xiaozhi.me/)** - 注册账号管理设备
- **[小智 ESP32 项目](https://github.com/78/xiaozhi-esp32)** - 官方固件项目
- **[WebSocket 协议文档](https://github.com/78/xiaozhi-esp32/blob/main/docs/websocket_zh.md)** - 协议规范
- **[WebRTC 音频处理](https://webrtc.org/native-code/audio/)** - 音频处理技术

### 🎉 总结

项目已经完成以下优化：

1. **OPUS 编解码** - 大幅降低带宽占用（20kbps）
2. **回声消除（AEC）** - 基于 WebRTC，实时消除扬声器回声
3. **噪音抑制（NS）** - 基于 WebRTC，有效过滤背景噪音
4. **自动增益控制（AGC）** - 自动调整麦克风音量
5. **自动对话流程** - 打开应用即可对话，无需手动操作
6. **静音检测** - 自动结束录音，提升用户体验
7. **自动重连** - 网络异常自动恢复
8. **官方服务器** - 直接连接小智官方服务器

**首次使用流程：** 打开应用 → 听验证码 → 在 xiaozhi.me 注册 → 重新打开应用即可正常使用！
